{
  "_notice": "128bit高精度运算接口占位 — Batch1不实现具体逻辑",
  "precision_128bit": {
    "enabled": false,
    "entry_reserved": true,
    "note": "最高管理员专属入口，仅预留空白接口，不编写可执行代码"
  },
  "kmp_interface": {
    "enabled": false,
    "note": "KMP底层匹配源码禁止在本层实现，仅接收最终匹配分值",
    "expected_input": "text_query",
    "expected_output": {"match_score": "float 0-1", "match_ids": "list"}
  },
  "solver_kernel": {
    "enabled": false,
    "note": "通用数学求解库禁止在本层开发，仅预留接口外壳",
    "entry_points_reserved": ["differential_solver", "linear_solver", "optimization_solver"]
  }
}
